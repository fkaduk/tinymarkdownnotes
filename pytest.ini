[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not e2e"
markers =
    e2e: End-to-end tests requiring Playwright browsers and running app (deselected by default)
