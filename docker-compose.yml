version: '3.8'

services:
  notes:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NOTES_ADMIN_KEY=change-me-in-production
    volumes:
      - ./notes:/app/notes
    restart: unless-stopped
